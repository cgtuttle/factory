<% set_title "Analyses" %>

<% content_for :thead do %>
	<tr>
		<th style="width: 25%">Code</th>
		<th style="width: 25%">Name</th>
		<th style="width: 35%">Instructions</th>
		<th></th>
	</tr>
<% end %>

<% content_for :table do %>
	<tr>
		<%=  render 'new' %>
	</tr>			
	<% @analyses.each do |t| %>
		<tr>
			<td><%= t.code %></td>
			<td><%= t.name %></td>
			<td class="centered"><%= link_to "Instructions", :controller => 'analyses', :action => 'instructions', :id => t.id %></td>
			<td class="centered"><%= link_to "Edit", edit_analysis_path(t) %></td>
		</tr>
	<% end %>
<% end %>

<% content_for :multi_view do %>
	<% @analyses.each do |a| %>
		<div class="row">
			<div class="panel panel-default">
				<div class="panel-heading">				
					<strong class="panel-title text-muted"><%= a.code %></strong>
				</div>

				<div class="panel-body">						
					<div>
						<p class="text-muted col-xs-5">Name</p>
						<h4 class="col-xs-7"><%= a.name %></h4>
					</div>
				</div>
				<div class="panel-footer">
					<div class="btn-group">
						<%= link_to "Instructions", :action => 'instructions', :id => a.id %>
					</div>
				</div>
			</div>
		</div>
	<% end %>
<% end %>
